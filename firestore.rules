rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /app_state/network {
      // Internal tool mode: the frontend uses the public Firebase config.
      allow read, write: if true;
    }
  }
}
